#define _CRT_SECURE_NO_WARNINGS 1/*屏蔽危险函数*/

#include<stdio.h>

int main()
{
	int num1=0;
	int num2=0;
	int num3=0;
	int sum = 0;
	scanf("%d%d%d", &num1, &num2, &num3);/*scanf为危险函数vs会报错*/
	sum = num1 + num2 + num3;
	printf("sum = %d\n",sum);
	return 0;
}
