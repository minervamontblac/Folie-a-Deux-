	//指针运算
	//指针+-整数
	//往后或往前跳一个数据类型地址
	//指针+-指针
	//得到是中间元素个数
	//两个指向不同空间的指针不讨论
	//指针关系运算
	//指针下标对比，允许数组指针与超出数组最后一个元素的后一个比较
	//指针与数组
	//数组名在绝大部分情况下是首元素的地址
	//1.&数组名--表示整个数组的地址
	//2.sizeof(数组名)--数组名表示整个数组，计算的是整个数组的大小
